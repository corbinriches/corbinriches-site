---
import Layout from '../../../layouts/Layout.astro';
import { getPublishedPosts, getTagSummaries } from '../../../utils/blog';

const posts = await getPublishedPosts();
const tags = getTagSummaries(posts);
---

<Layout title="Writing Tags" description="Browse writing by tag.">
	<section class="page-header">
		<p class="eyebrow">writing/tags</p>
		<h1>Tag index.</h1>
		<p>A small index for browsing notes by topic.</p>
	</section>

	<section class="section">
		{
			tags.length > 0 ? (
				<div class="tag-index">
					{tags.map((tag) => (
						<a class="tag-index-link" href={`/blog/tags/${tag.slug}/`}>
							<span>{tag.tag}</span>
							<span>{tag.count} {tag.count === 1 ? 'post' : 'posts'}</span>
						</a>
					))}
				</div>
			) : (
				<div class="empty-state">
					<p>No published tags yet.</p>
				</div>
			)
		}
	</section>

	<p class="post-back"><a href="/blog/">back to writing</a></p>
</Layout>
